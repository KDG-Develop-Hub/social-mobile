import 'package:flutter/material.dart';

class MitukeruScreen extends StatelessWidget {
  const MitukeruScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('見つける'),
      ),
    );
  }
}
