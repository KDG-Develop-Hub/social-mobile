import 'package:flutter/material.dart';

class ApudeScreen extends StatelessWidget {
  const ApudeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('アプデ'),
      ),
    );
  }
}
