import 'package:flutter/material.dart';

class SetteScreen extends StatelessWidget {
  const SetteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('せってー'),
      ),
    );
  }
}
