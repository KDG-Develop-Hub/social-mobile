import 'package:flutter/material.dart';

class KimitohaScreen extends StatelessWidget {
  const KimitohaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('君とは'),
      ),
    );
  }
}
